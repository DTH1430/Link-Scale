`EN`
# A simple landing page template to showcase different website design styles

## How to run locally

**Prerequisites:**  Node.js
1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Click on the Style button to choose different styles

---

`VN`
# Một landing page đơn giản để giới thiệu các phong cách thiết kế trang web khác nhau

## Cách chạy trên local

**Cần có:** Node.js
1. Cài đặt các dependencies:
`npm install`
2. Chạy ứng dụng:
`npm run dev`
3. Nhấn vào nút Style để chọn các phong cách khác nhau

